const noop = () => {};
export default noop;


  /*
  getProperties(obj) {
    const objProperties = [];
    for (const name in obj) {
      if (obj.hasOwnProperty(name)) {
        objProperties.push(`name: ${name} - value: ${obj[name]}`);
      }
    }
    return objProperties;
  }
  */
